{% extends 'base.html.twig' %}

{% block title %}Inscrire un élève à un cours
{% endblock %}

{% block body %}
    {# titre #}
	<div class="d-flex justify-content-center text-center row px-5 mx-1">
		<h1 class="mt-3">Inscription à un cours</h1>
	</div>

    {# formulaire #}
	<div class="mx-3">
		<div class="d-flex ">
			{{ include('inscrit/_form.html.twig') }}
		</div>

        {# Bouton de retour #}
		<div class="mt-5">
			<a class="btn btn-primary mt-2" href="{{ path('app_inscrit_index') }}">Retour à la liste des cours</a>
		</div>
	</div>
{% endblock %}


{% extends 'base.html.twig' %}

{% block title %}Actualiser la présence de l'élève
{% endblock %}

{% block body %}

	{# titre #}
	<div class="d-flex justify-content-center text-center row px-5 mx-1">
		<h1 class="mt-3">Actualiser la présence de l'élève</h1>
	</div>

	{# formulaire #}
	<div class="mx-3">
		<p class="mx-2">Actuellement
			{{ presence.coursPrenomEleve }}
			{{ presence.coursNomEleve }}
		</p>
		<div class="d-flex mx-2">
			{{ include('presence/_form.html.twig', {'button_label': 'Modifier'}) }}
		</div>

		{# Bouton de retour #}
		<div class="mt-5 d-flex ">
			<a class="btn btn-primary mt-2 mx-2" href="{{ path('app_presence_index') }}">Retour à la liste des cours</a>

            {{ include('presence/_delete_form.html.twig') }}
		</div>

		
	</div>
{% endblock %}

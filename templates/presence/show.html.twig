{% extends 'base.html.twig' %}

{% block title %}Presence{% endblock %}

{% block body %}
    <h1>Presence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ presence.id }}</td>
            </tr>
            <tr>
                <th>Date_cours</th>
                <td>{{ presence.dateCours ? presence.dateCours|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Validation_presence</th>
                <td>{{ presence.validationPresence }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_presence_index') }}">back to list</a>

    <a href="{{ path('app_presence_edit', {'id': presence.id}) }}">edit</a>

    {{ include('presence/_delete_form.html.twig') }}
{% endblock %}

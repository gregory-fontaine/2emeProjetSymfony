{% extends 'base.html.twig' %}

{% block title %}Edit Utilisateur{% endblock %}

{% block body %}
    <h1>Edit Utilisateur</h1>



{# 
    {{form_start(formEditUser)}}
    {{ form_row(formEditUser.prenom_utilisateur) }}
    {{ form_row(formEditUser.nom_utilisateur) }}
    <div class="hidden">{{ form_row(formEditUser.plainPassword) }}</div>
    {{form_end(formEditUser)}} #}

    form de base
    {{ include('utilisateur/_formEditUser.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_utilisateur_index') }}">back to list</a>

    {{ include('utilisateur/_delete_form.html.twig') }}
{% endblock %}
